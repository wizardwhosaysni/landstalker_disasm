; Entity script added after vanilla ones : id 920 / $398

        dc.b    $68     ; New behaviour command "UpdatePlayerShadow"
        dc.b    $12     ; GotoCommand
        dc.b    $FF     ; 1 byte backwards